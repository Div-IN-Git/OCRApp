FIRST TIME SETUP ---IMPORTANT

open consent screen
select your browser --wait

============== STEP 1 ======================
on the google cloud console screen
	
	Create a new project(name it anything)
	on the left side click the three lines on top left corner
	click enable apis option (it will show consent screenn is not setup)
	click to set consent screen
	name it OCRapp
	set it to EXTERNAL
	give your email
	DESKTOP app
	
DONE

============= STEP 2 ========================
On the left three lines click AUDIENCE
	+ADD TEST USER
	ADD YOUR EMAIL
	click save TWO TIME(Important)

DONE

============= STEP 3 ========================
click on the api option again from left three line
search: Google Drive api
        Google Docs api

select and enable the first ones(WAIT AFTER CLICKING ENABLE ONE EACH ONE)

============= STEP 4 ========================
CLICK YOUR PROJECT 
	DOWNLOAD JSON FILE (credentials or client_secret)
	SELECT OAuth file from dropdown menu (NOT API)
CLOSE THE WEBSITE
DONE

============ STEP 5 =========================
INSIDE APP
	CLICK IMPORT CREDENTIALS
	select the client_secret.JSON file (example: client_secret_8270ejjfpqyr093rh)
	open

IF: Credentials imported sucessfully
DONE

use the app



	 